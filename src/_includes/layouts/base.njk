<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title or homepage.meta.title }}</title>
  <meta name="description" content="{{ description or homepage.meta.description }}">

  <link href="{{pathPrefix}}/styles/style.css" rel="stylesheet" />
  <script src="{{pathPrefix}}/scripts/main.js"></script>

</head>
<body class="bg-gray-100 text-gray-900">
  {% include "components/header.njk" %}
  <main>
    {{ content | safe }}
  </main>
  {% include "components/footer.njk" %}
</body>
</html>
